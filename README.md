# SimuMatch — Athlete ↔ Event Matching MVP


This repository contains an MVP for SimuMatch: synthetic data generator, preprocessing, baseline model training (XGBoost), and a FastAPI server that returns top-K event matches for a given athlete profile.


Follow the Quickstart in the canvas to run locally.
